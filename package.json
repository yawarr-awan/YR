{
  "name": "yr-wellness",
  "private": true,
  "version": "1.22.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "wrangler": "^4.0.0"
  }
}
